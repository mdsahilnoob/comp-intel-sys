# Shared packages

This directory is reserved for future shared libraries and configuration packages. The initial migration keeps the existing frontend design system inside `apps/web` so its import paths and visual behavior remain unchanged.
